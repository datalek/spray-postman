server.port = 8000
server.host = "localhost"

akka {
	loglevel = INFO
	log-dead-letters = off
	log-dead-letters-during-shutdown = off
}

spray.can.server {
	request-timeout = 1s
}

mongodb {
	uri = "mongodb://application:application@localhost:27017/postman-test"
}

mandrill {
	key = "put here the key"
}